diff --git a/.loop-engineering/iterations/001/context.md b/.loop-engineering/iterations/001/context.md
index a0a2ef2..433a0fe 100644
--- a/.loop-engineering/iterations/001/context.md
+++ b/.loop-engineering/iterations/001/context.md
@@ -1,11 +1,6 @@
 # Context
 
 - objective: Replace HLP-007 time and padding wrappers with Kujo-native helpers while retaining the intentionally ASCII slug policy.
-- git_head: ebd62ec
+- git_head: 28741a0
 - changed_files:
-  - src/cli.kujo
-  - src/record.kujo
-  - src/render.kujo
-  - src/storage.kujo
-  - src/util.kujo
-  - tests/runledger_test.kujo
+  - .loop-engineering/loop.yml
diff --git a/.loop-engineering/iterations/001/diff.patch b/.loop-engineering/iterations/001/diff.patch
index bdc896a..e69de29 100644
--- a/.loop-engineering/iterations/001/diff.patch
+++ b/.loop-engineering/iterations/001/diff.patch
@@ -1,192 +0,0 @@
-diff --git a/src/cli.kujo b/src/cli.kujo
-index 8cf2863..a3aefbe 100644
---- a/src/cli.kujo
-+++ b/src/cli.kujo
-@@ -9,7 +9,7 @@
- #   1  operational failure (no such run, invalid status, corrupt file)
- #   2  usage error (bad/missing arguments)
- 
--from src.util import iso_now, truthy
-+from src.util import truthy
- from src.record import new_record, is_valid_status, valid_statuses, empty_usage, empty_cost
- from src.storage import generate_id, save_new, save_update, load, list_records, run_path, write_atomic_text
- from src.gitmeta import start_metadata, end_metadata
-@@ -152,7 +152,7 @@ func resolve_ledger(parsed) {
- 
- # Refresh a record's updated_at timestamp.
- func touch(record) {
--    record["updated_at"] := iso_now()
-+    record["updated_at"] := now_utc()
-     return record
- }
- 
-@@ -269,7 +269,7 @@ func cmd_finish(tokens) {
-         return usage_error(usage("finish"))
-     }
-     if type(notes) != "null" {
--        rec["notes"] := push(rec["notes"], {"at": iso_now(), "text": notes})
-+        rec["notes"] := push(rec["notes"], {"at": now_utc(), "text": notes})
-     }
- 
-     repo := flag_or(parsed, "repo", rec["repo_path"])
-@@ -355,7 +355,7 @@ func cmd_note(tokens) {
-     if type(text) == "null" || text == "" {
-         return usage_error(usage("note-short"))
-     }
--    rec["notes"] := push(rec["notes"], {"at": iso_now(), "text": text})
-+    rec["notes"] := push(rec["notes"], {"at": now_utc(), "text": text})
-     touch(rec)
-     res := save_update(ledger, rec)
-     if has_key(res, "error") == 1 {
-@@ -384,7 +384,7 @@ func cmd_followup(tokens) {
-     if type(text) == "null" || text == "" {
-         return usage_error(usage("followup-short"))
-     }
--    rec["followups"] := push(rec["followups"], {"at": iso_now(), "text": text})
-+    rec["followups"] := push(rec["followups"], {"at": now_utc(), "text": text})
-     touch(rec)
-     res := save_update(ledger, rec)
-     if has_key(res, "error") == 1 {
-diff --git a/src/record.kujo b/src/record.kujo
-index ccbb394..3a81806 100644
---- a/src/record.kujo
-+++ b/src/record.kujo
-@@ -4,8 +4,6 @@
- # its default/empty values, and validation of the small set of enumerated
- # fields (status). Everything is a plain dict so it serializes directly to JSON.
- 
--from src.util import iso_now
--
- # Allowed run statuses. `in_progress` is the implicit start state; the rest are
- # terminal verdicts a human assigns when finishing a run.
- export func valid_statuses() {
-@@ -48,7 +46,7 @@ export func empty_cost() {
- # storage layer mints the id, the CLI supplies provider/model/task/etc.).
- # Git fields default to null and are filled in defensively by the git layer.
- export func new_record(id, provider, model, task_name, prompt_file, repo_path) {
--    ts := iso_now()
-+    ts := now_utc()
-     return {
-         "id": id,
-         "created_at": ts,
-diff --git a/src/render.kujo b/src/render.kujo
-index 62c97fc..339a727 100644
---- a/src/render.kujo
-+++ b/src/render.kujo
-@@ -9,7 +9,7 @@
- # Kujo compile path rejects more than one `for` loop per function scope, so the
- # index form keeps the whole module compiling cleanly under `kujo check`.
- 
--from src.util import pad_right, display, display_or
-+from src.util import display, display_or
- from src.record import changed_file_count, followup_count, valid_statuses
- 
- # Human-friendly cost string for a record's cost block: "1.25 USD" or "-".
-@@ -70,7 +70,7 @@ func render_row(cells, widths) {
-     mut parts := []
-     mut i := 0
-     while i < len(cells) {
--        parts = push(parts, pad_right("${cells[i]}", widths[i]))
-+        parts = push(parts, pad_right("${cells[i]}", widths[i], " "))
-         i = i + 1
-     }
-     return join(parts, "  ")
-diff --git a/src/storage.kujo b/src/storage.kujo
-index fcb8a1f..7223619 100644
---- a/src/storage.kujo
-+++ b/src/storage.kujo
-@@ -5,7 +5,7 @@
- # function takes the ledger directory explicitly so callers stay in control of
- # where the ledger lives (default ./.runledger, overridable via CLI/env).
- 
--from src.util import slug, today_utc, pad_int, truthy
-+from src.util import slug, pad_int, truthy
- from src.record import empty_usage, empty_cost
- 
- # Path to the directory holding individual run files.
-@@ -118,7 +118,7 @@ export func exists(ledger_dir, id) {
- # where NNN is the next free 3-digit sequence for that exact prefix. This keeps
- # ids sortable, readable, and collision-free within a ledger.
- export func generate_id(ledger_dir, model, task_name) {
--    prefix := today_utc() + "-" + slug(model) + "-" + slug(task_name)
-+    prefix := format_date(now_unix(), "YYYY-MM-DD") + "-" + slug(model) + "-" + slug(task_name)
-     mut seq := 1
-     while seq < 1000 {
-         candidate := prefix + "-" + pad_int(seq, 3)
-diff --git a/src/util.kujo b/src/util.kujo
-index a344e42..06cf6cc 100644
---- a/src/util.kujo
-+++ b/src/util.kujo
-@@ -37,16 +37,6 @@ export func slug(text) {
-     return out
- }
- 
--# Current time as an ISO-8601 UTC timestamp, e.g. 2026-05-29T17:08:42Z.
--export func iso_now() {
--    return format_date(now(), "YYYY-MM-DDTHH:mm:ssZ")
--}
--
--# Current calendar date in UTC, e.g. 2026-05-29.
--export func today_utc() {
--    return format_date(now(), "YYYY-MM-DD")
--}
--
- # Zero-pad an integer to `width` digits (used for run sequence numbers).
- export func pad_int(value, width) {
-     s := to_string(value)
-@@ -57,16 +47,6 @@ export func pad_int(value, width) {
-     return out
- }
- 
--# Right-pad a string with spaces to `width` for fixed-width table columns.
--# Strings longer than width are returned unchanged (no truncation surprises).
--export func pad_right(text, width) {
--    mut out := text
--    while len(out) < width {
--        out = out + " "
--    }
--    return out
--}
--
- # Display a value for human-readable output, mapping null to a dash.
- export func display(value) {
-     if type(value) == "null" {
-diff --git a/tests/runledger_test.kujo b/tests/runledger_test.kujo
-index de4eeaa..5c9478e 100644
---- a/tests/runledger_test.kujo
-+++ b/tests/runledger_test.kujo
-@@ -13,7 +13,7 @@
- # the current runtime, so this hand-rolled harness is used instead. It runs on
- # the standard `kujo run` path where module imports work normally.
- 
--from src.util import slug, iso_now, pad_int
-+from src.util import slug, pad_int
- from src.record import new_record, is_valid_status, valid_statuses, empty_usage, empty_cost, changed_file_count, followup_count
- from src.storage import generate_id, save_new, save_update, load, list_records, exists, ensure_ledger, run_path, is_safe_run_id
- from src.gitmeta import is_repo, head_commit, is_dirty, changed_files, start_metadata, end_metadata
-@@ -73,8 +73,8 @@ func test_util() {
-     check_eq(slug("Build RunLedger!"), "build-runledger", "slug strips punctuation")
-     check_eq(slug("claude-opus-4-8"), "claude-opus-4-8", "slug preserves model name")
-     check_eq(pad_int(7, 3), "007", "pad_int zero pads")
--    check(len(iso_now()) == 20, "iso_now is 20 chars (….Z)")
--    check(ends_with(iso_now(), "Z"), "iso_now ends with Z")
-+    check(len(now_utc()) == 20, "now_utc is 20 chars (….Z)")
-+    check(ends_with(now_utc(), "Z"), "now_utc ends with Z")
- }
- 
- # --- record: shape + validation ----------------------------------------
-@@ -164,12 +164,12 @@ func test_lifecycle() {
-     check(changed_file_count(reloaded) >= 1, "changed files detected on finish")
- 
-     # notes
--    rec["notes"] := push(rec["notes"], {"at": iso_now(), "text": "a note"})
-+    rec["notes"] := push(rec["notes"], {"at": now_utc(), "text": "a note"})
-     save_update(LEDGER, rec)
-     check_eq(len(load(LEDGER, id)["notes"]), 1, "note added and persisted")
- 
-     # followups
--    rec["followups"] := push(rec["followups"], {"at": iso_now(), "text": "a followup"})
-+    rec["followups"] := push(rec["followups"], {"at": now_utc(), "text": "a followup"})
-     save_update(LEDGER, rec)
-     check_eq(followup_count(load(LEDGER, id)), 1, "followup added and persisted")
- 
diff --git a/.loop-engineering/loop.yml b/.loop-engineering/loop.yml
index 010aaf1..4f86f85 100644
--- a/.loop-engineering/loop.yml
+++ b/.loop-engineering/loop.yml
@@ -17,7 +17,7 @@ blocked_actions:
 
 eval_gates:
   - id: tests
-    command: "KUJO_BIN=${KUJO_BIN:-../kujo/target/debug/kujo} ./tests/run.sh"
+    command: "KUJO=${KUJO:-../kujo/target/debug/kujo} ./tests/run.sh"
     required: true
   - id: diff_check
     command: "git diff --check"
